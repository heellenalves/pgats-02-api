{
  "name": "pgats-02-api",
  "version": "1.0.0",
  "description": "API para aprendizado de testes e automação a nível de API.",
  "main": "server.js",
  "scripts": {
    "test": "mocha test/**/*.test.js --timeout 10000",
    "test-external": "mocha test/external/*.test.js --timeout 10000",
    "test-controller": "mocha test/controller/*.test.js --timeout 10000",
    "start": "node server.js"
  },
  "keywords": [
    "api",
    "express",
    "swagger",
    "testes",
    "automacao"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "chai": "4.5.0",
    "express": "^4.18.2",
    "mocha": "^11.7.2",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4",
    "swagger-ui-express": "^5.0.1"
  }
}
